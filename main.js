class Game {constructor() {this.canvas = document.getElementById('gameCanvas'); this.scene = new THREE.Scene(); this.camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000); this.renderer = new THREE.WebGLRenderer({canvas: this.canvas, antialias: true}); this.renderer.setSize(window.innerWidth, window.innerHeight); this.renderer.setClearColor(0x87ceeb); this.world = new World(this.scene, this.renderer); this.player = new Player(this.scene, this.world); this.isRunning = false; this.clock = new THREE.Clock(); this.setupLights(); this.setupEventListeners();} setupLights() {const sunLight = new THREE.DirectionalLight(0xffffff, 0.8); sunLight.position.set(100, 100, 50); sunLight.name = 'sunLight'; this.scene.add(sunLight); const ambientLight = new THREE.AmbientLight(0xffffff, 0.4); this.scene.add(ambientLight);} setupEventListeners() {window.addEventListener('resize', () => this.onWindowResize()); document.addEventListener('click', () => this.canvas.requestPointerLock?.());} startGameLoop() {this.isRunning = true; this.gameLoop();} gameLoop = () => {if (!this.isRunning) return; requestAnimationFrame(this.gameLoop); const deltaTime = this.clock.getDelta(); this.player.update(deltaTime); this.world.updateDayNightCycle(deltaTime); this.renderer.render(this.scene, this.player.camera);} onWindowResize() {this.camera.aspect = window.innerWidth / window.innerHeight; this.camera.updateProjectionMatrix(); this.renderer.setSize(window.innerWidth, window.innerHeight);}} const game = new Game(); window.game = game;